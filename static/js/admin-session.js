/**
 * Admin session JavaScript for FinArva AI
 *
 * This is an empty file that replaces the original admin-session.js
 * to prevent the redirect loop when logging into the admin site.
 */

// We're no longer checking for cookies or redirecting to login
// This file remains to avoid 404 errors but doesn't do anything